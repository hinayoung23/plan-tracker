{
  "id": "plan-tracker",
  "name": "Plan Tracker",
  "description": "Long-term plan tracking with milestones, check-ins, progress analysis, webhook push notifications, and event-scheduled reminders. MCP server for AI assistants.",
  "version": "1.6.3",
  "activation": {
    "onStartup": false
  },
  "contracts": {
    "mcpServers": {
      "plan-tracker": {
        "command": "plan-tracker-mcp",
        "args": [],
        "env": {}
      }
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "pythonPath": {
        "type": "string",
        "description": "Path to Python interpreter (default: auto-detect via setup command)"
      }
    }
  }
}
